import 'package:flutter/material.dart';

class NearestHospitalsScreen extends StatelessWidget {
  const NearestHospitalsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
